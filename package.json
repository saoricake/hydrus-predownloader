{
  "name": "hydrus-downloader",
  "packageManager": "yarn@4.6.0",
  "scripts": {
    "build": "powershell ./build.ps1",
    "start": "yarn build && web-ext run -f=deved -p=default"
  },
  "devDependencies": {
    "@types/firefox-webext-browser": "^143",
    "typescript": "^5.9"
  }
}
